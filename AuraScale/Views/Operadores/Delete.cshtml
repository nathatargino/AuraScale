@model AuraScale.Models.Operador

@{
    ViewData["Title"] = "Excluir Operador";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0"><i class="fa fa-exclamation-triangle me-2"></i> Confirmar Exclusão</h5>
                </div>
                <div class="card-body text-center py-4">
                    <p class="mb-4">Tem certeza que deseja remover este operador da sua equipe?</p>

                    <div class="bg-body p-3 rounded-3 mb-4">
                        <h4 class="fw-bold mb-1">@Model.Nome</h4>
                        <p class="text-muted mb-0">@Model.Funcao</p>
                        @if (!string.IsNullOrEmpty(Model.Email))
                        {
                            <small class="text-secondary">@Model.Email</small>
                        }
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />

                        <div class="d-flex justify-content-center gap-2">
                            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">
                                Cancelar
                            </a>
                            <button type="submit" class="btn btn-danger rounded-pill px-4 shadow-sm">
                                <i class="fa fa-trash me-2"></i> Confirmar Exclusão
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="text-center mt-3">
                <small class="text-muted">
                    Esta ação não pode ser desfeita. O colaborador perderá o acesso às escalas vinculadas.
                </small>
            </div>
        </div>
    </div>
</div>